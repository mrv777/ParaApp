diff --git a/android/src/main/java/com/reactnativeandroidwidget/RNWidgetImageProvider.java b/android/src/main/java/com/reactnativeandroidwidget/RNWidgetImageProvider.java
index 745ef894d7d1edaa6b925afaf47448123d8df4b0..5860a3df1445f48dfe3e54674eaeadfece07ec5a 100644
--- a/android/src/main/java/com/reactnativeandroidwidget/RNWidgetImageProvider.java
+++ b/android/src/main/java/com/reactnativeandroidwidget/RNWidgetImageProvider.java
@@ -14,7 +14,6 @@ import java.io.File;
 import java.io.FileNotFoundException;
 import java.io.FileOutputStream;
 import java.io.IOException;
-import java.util.Objects;
 
 public class RNWidgetImageProvider extends ContentProvider {
     public static final String AUTHORITY_SUFFIX = ".rnwidget.imageprovider";
@@ -78,7 +77,13 @@ public class RNWidgetImageProvider extends ContentProvider {
         File folder = getFolderWithImages(context);
         File[] files = folder.listFiles(pathname -> pathname.getName().startsWith(prefix));
 
-        for (File f : Objects.requireNonNull(files)) {
+        // listFiles() returns null when the folder is absent or unreadable, which happens
+        // when a widget is removed before any render wrote an image, or after app data was
+        // cleared. Deletion runs in AppWidgetProvider.onDeleted, so throwing here crashes
+        // the app process and skips the WIDGET_DELETED task.
+        if (files == null) return;
+
+        for (File f : files) {
             f.delete();
         }
     }
